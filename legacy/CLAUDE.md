# legacy/ — sub-agent brief
Superseded generations, kept for provenance. NEVER import from here; never edit
legacy/snapshots/ (self-documenting locks — their READMEs are the model-lineage docs
and contain the pending Option A/B experiment plan in fixedGrid_v1).

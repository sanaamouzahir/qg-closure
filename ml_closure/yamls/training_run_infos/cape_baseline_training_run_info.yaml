# WHAT THIS FILE IS: the dataset and training manifest for the cape baseline model: five inlet types pooled, unconditioned, 100 epochs (run codename cape_base_100ep).
# CONTENTS: which simulation members were trained on (runs), training/
# validation frame and pixel counts, valid pixels per frame (after removing
# sponge zones and the obstacle body), which member was held out if any
# (lomo_holdout), learning rate / weight decay / epochs, whether FiLM
# inlet-speed conditioning is on, and the Gaussian-process initialization
# constants (including y_mean/y_std used to standardize the target).
# COPY made 2026-07-13 during the diagnostics-library reorganization. The original file in runs_piff/ is untouched (running jobs still write there).
# Original: runs_piff/cape_base_100ep/run_info.yaml
#
seed: 0
runs:
- FPCape-const
- FPCape-sine
- FPCape-ramp
- FPCape-ou
- FPCape-tel
n_train_frames: 780
n_val_frames: 220
N_train_pixels: 145020720
N_val_pixels: 40903280
valid_per_frame:
  FPCape-const: 185924
  FPCape-sine: 185924
  FPCape-ramp: 185924
  FPCape-ou: 185924
  FPCape-tel: 185924
lomo_holdout: null
lr: 0.001
weight_decay: 1.0e-05
epochs: 100
film: true
ELBO_num_data: 145020720
device: cuda
init_hyperparams:
  gp_mean_init: 0.0
  gp_space_var: 1.0
  noise_frac: 0.1
  outputscale_init: 0.9
  noise_init: 0.1
  y_mean: 1.5640475606140234
  y_std: 281.3809817234043
  stats_n_pixels: 145020720

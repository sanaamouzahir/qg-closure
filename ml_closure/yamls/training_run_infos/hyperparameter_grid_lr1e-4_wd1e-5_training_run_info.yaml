# WHAT THIS FILE IS: the dataset and training manifest for hyperparameter-grid rung: learning rate 1.0e-4, weight decay 1.0e-5, 60 epochs, cylinder steady inlet (run codename grid_lr1.0e-4_wd1.0e-5).
# CONTENTS: which simulation members were trained on (runs), training/
# validation frame and pixel counts, valid pixels per frame (after removing
# sponge zones and the obstacle body), which member was held out if any
# (lomo_holdout), learning rate / weight decay / epochs, whether FiLM
# inlet-speed conditioning is on, and the Gaussian-process initialization
# constants (including y_mean/y_std used to standardize the target).
# COPY made 2026-07-13 during the diagnostics-library reorganization. The original file in runs_piff/ is untouched (running jobs still write there).
# Original: runs_piff/grid_lr1.0e-4_wd1.0e-5/run_info.yaml
#
seed: 0
runs:
- FPC-const
n_train_frames: 259
n_val_frames: 74
N_train_pixels: 62548500
N_val_pixels: 17871000
valid_per_frame:
  FPC-const: 241500
lomo_holdout: null
lr: 0.0001
weight_decay: 1.0e-05
epochs: 60
film: true
ELBO_num_data: 62548500
device: cuda
init_hyperparams:
  gp_mean_init: 0.0
  gp_space_var: 1.0
  noise_frac: 0.1
  outputscale_init: 0.9
  noise_init: 0.1
  y_mean: -0.001354597939620299
  y_std: 87.27759533841362
  stats_n_pixels: 62548500
